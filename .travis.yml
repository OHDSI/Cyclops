language: cpp
compiler:
    - gcc
    - clang
before_install:
script:
    - mkdir -p standalone/build && (cd standalone/build && cmake .. && make)
notifications:
  recipients:
    - msuchard@gmail.com
  email:
    on_success: change
    on_failure: always 
    